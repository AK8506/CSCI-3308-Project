<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-image: url('https://www.coloradoski.com/wp-content/uploads/2020/09/8H0A2921-scaled.jpg');
      background-size: cover;
      background-repeat: no-repeat;
      background-position: center;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .navbar {
      display: flex;
      justify-content: flex-start;
      align-items: center;
      background-color: rgba(0, 0, 0, 0.7);
      padding: 10px 20px;
      position: fixed;
      top: 0;
      width: 100%;
      z-index: 10;
    }

    .navbar a,
    .navbar button {
      margin-right: 10px;
    }

    .greeting {
      color: white;
      margin: 80px 0 10px 20px;
      font-size: 1.25rem;
    }
  </style>
</head>
<body>

  <!-- Navigation bar -->
  <div class="navbar">
    {{#if loggedIn}}
      <a href="/" class="btn btn-light">Home</a>
      <form action="/logout" method="POST" style="display:inline;">
        <button type="submit" class="btn btn-light">Logout</button>
      </form>
    {{else}}
      <a href="/login" class="btn btn-light">Login</a>
      <a href="/register" class="btn btn-light">Register</a>
    {{/if}}
  </div>

  <!-- Optional greeting -->
  {{#if loggedIn}}
    <div class="greeting">
      Hello, {{username}}!
    </div>
  {{/if}}

  <!-- Main content -->
  <div style="margin-top: 60px; flex: 1;">
    {{{body}}}
  </div>

</body>
</html>
